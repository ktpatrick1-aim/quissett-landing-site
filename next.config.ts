import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Images are served locally from /public/images — no remote patterns needed.
  // When Kevin provides new photos, drop them in /public/images/ and update references.
};

export default nextConfig;
